
language: c

before_install:
  - "sudo apt-get install -qq mingw-w64"
  - "sudo apt-get install -qq mingw32"

before_script:
  - "uname -a"
  - "uname -r"
  - "CC=$XCC ./configure"

script:
  - "make"
  - "make dist"

